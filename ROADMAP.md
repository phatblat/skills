# Roadmap

- Archive `phatblat/changelog-skill`, `phatblat/semver-skill` and
  `phatblat/portable-skill-authoring` with a README pointer here.
- Wire `.codex-plugin/plugin.json`'s `version` into the release before
  submitting the plugin to any Codex marketplace; it is a placeholder today.
- Revisit applying to Claude Code's official marketplace once the skills have
  external users.
